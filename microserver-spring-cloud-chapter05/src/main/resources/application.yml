spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/wcjwctwy/SpringcloudConfig
          search-paths: respo
      label: master
server:
  port: 8888
